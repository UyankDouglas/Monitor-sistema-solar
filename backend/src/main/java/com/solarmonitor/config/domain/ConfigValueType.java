package com.solarmonitor.config.domain;

/** Tipo lógico do valor de uma configuração (para conversão/validação). */
public enum ConfigValueType {
    STRING,
    INT,
    DECIMAL,
    BOOLEAN,
    JSON
}
